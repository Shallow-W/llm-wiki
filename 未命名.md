# AgentHub 答辩稿：分模块精简版

## 开场介绍

各位老师好，我们的项目叫 **AgentHub**，它是一个 **IM 驱动的多 Agent 协作平台**。

我们想解决的问题是：现在很多 CLI Agent，比如 Claude Code、Codex、OpenCode，本身能力已经很强，但是彼此之间不能方便的进行通信，
但它们大多还是运行在命令行里。用户需要切换终端、手动管理上下文，也很难让多个 Agent 像团队成员一样协作。

所以 AgentHub 做的事情是：**把本地 CLI Agent 封装成聊天系统里的联系人**。用户可以在单聊里直接找某个 Agent，也可以在群聊里通过 @mention 让多个 Agent 分工协作。系统背后负责本地 Agent 接入、消息持久化、多 Agent 调度、上下文构建和产物预览。

我认为这个项目最有工程含量的地方，不是某一个页面，而是几个关键抽象组合在一起，形成了一套完整的协作调度链路。

---

## 模块一：Agent 适配层设计

第一个模块是 **Agent 适配层**。

这个模块主要解决的是：怎么把用户本地已经安装好的 CLI Agent 接入平台。

我们的设计不是把代码执行放到云端，而是在用户本机运行一个 **Daemon**。Daemon 启动后会扫描本机环境，识别 Claude Code、Codex、OpenCode 这些 CLI 是否可用，然后通过独立的 Daemon WebSocket 连接后端。

这样做有一个好处：Agent 可以直接复用用户本地已经登录、已经授权、已经配置好的开发环境，也可以直接在本地项目目录里执行任务。

这里有一个比较小但很关键的设计，就是 **Agent ID × Slot × Session** 的拆分。

**Agent ID** 是平台里的身份，表示“这是哪个 Agent”。  
**Slot** 是 Daemon 里的本地进程槽位，用来控制同一个 Agent 不要同时跑多个任务。  
**Session ID** 是按照 `agent × conversation` 维度隔离的上下文，保证同一个 Agent 在不同会话里不会串上下文。

所以 Server 主要管理用户、Agent、会话和消息；Daemon 主要管理本地进程和 Session 映射；CLI Agent 自己继续管理自己的本地历史。

这个拆分让平台身份、本地进程和上下文隔离各自独立，后面做多 Agent 调度时会更稳定。

---

## 模块二：IM 聊天核心与调度入口

第二个模块是 **IM 聊天核心**。

我们没有让用户直接面对复杂的调度配置，而是使用大家熟悉的聊天范式。用户在对话列表里选择单聊或群聊，在输入框里 @某个 Agent，系统就能知道这条消息要发给谁处理。

这个模块的功能看起来像普通聊天，但它在系统里其实是 **调度入口**。

这里我们做了一个边界设计：**IM 层只负责产生结构化信息，不直接执行 Agent**。

也就是说，前端主要提交 `messageId`、`conversationId` 和 `mention targets`。后端先把消息落库，然后再进入后面的路由、上下文构建和任务派发。

这个设计的好处是，聊天体验和调度策略解耦。以后无论是单 Agent 直达、多 Agent 并行，还是 Orchestrator 统一协调，前端都不需要大改。

同时，流式回复、失败、停止、完成状态都会回到同一个消息流里。用户只需要在聊天界面里看任务过程，不需要切换到终端或其他页面。

---

## 模块三：多 Agent 派发引擎

第三个模块是 **多 Agent 派发引擎**，也是我们项目里工程含量比较高的一部分。

在简单场景下，用户 @一个 Agent，系统直接派发就可以。但在群聊里，用户可能同时 @多个 Agent，可能需要有人拆任务，也可能需要多个 Agent 并行工作，最后再汇总结果。

所以我们设计了几个抽象：**Orchestrator、DispatchPlan、Dispatcher、AgentQueue 和 DaemonHub**。

**Orchestrator** 负责理解群聊里的协作意图。比如用户 @了多个 Agent，第一个 Agent 可以作为协调者，先拆解任务，再分发给其他 worker。

**DispatchPlan** 是统一的派发计划。它把“要让谁做、带什么上下文、如何处理结果”封装起来。这样无论是单聊、群聊、动态 Orchestrator，还是固定 Orchestrator，后面都可以走统一的派发流程。

**Dispatcher** 负责真正的任务执行链路，包括创建 task、通过 WebSocket 派发给 Daemon、等待执行结果，以及把结果写回消息流。

这里另一个关键设计是 **AgentQueue**。

因为 CLI Agent 通常会复用本地上下文，如果同一个 Agent 同时执行多个任务，容易出现上下文污染。所以我们的策略是：**同一个 Agent 串行执行，不同 Agent 可以并行执行**。

这样既保证了本地 CLI 执行的稳定性，又保留了多 Agent 协作的并发能力。

这一模块可以总结为：AgentHub 不是简单把 @消息转发给 Agent，而是把路由、上下文、任务派发、并发控制和结果回写做成了一套统一调度机制。

---

## 模块三补充：ContextChain 上下文工程

在派发之前，还有一个很重要的问题：Agent 到底应该拿到什么上下文？

如果每次都把整个聊天历史塞给 Agent，会有很多噪声；如果每条路径都手写 prompt，代码又会很乱。

所以我们把上下文构建抽象成 **ContextChain**。

它的设计是：不同任务路径走不同的上下文链。比如单 Agent 直答、worker 执行、Orchestrator 拆解、fanout 并行和 summary 总结，它们需要的上下文不完全一样。

每条链由多个 Builder 组成，比如 AgentConfig、知识库、黑板 Pin、附件、Orchestrator Prompt 等。每个 Builder 只负责一种上下文来源。

这个设计的亮点是：**上下文工程从 Orchestrator 里抽离出来了**。

Orchestrator 不需要自己拼大段 prompt，它只负责选择协作路径。以后如果要新增知识库、文件、项目规范或任务看板，只需要新增 Builder 或调整 ContextChain，不需要改主调度流程。

---

## 模块四：卡片信息流与产物预览

第四个模块是 **卡片信息流和多类型产物预览**。

传统 Agent 的输出通常是一大段文本，但真实开发中，用户真正关心的是代码 diff、任务计划、网页预览、文档、PPT 这些具体产物。

所以我们设计了 **产物卡片协议**。

这里的核心设计是：**Agent 只产结构化数据，不负责前端渲染**。

Agent 可以输出 fenced block、card 数据或 artifact 信息；后端负责解析和保存结构化结果；前端根据卡片类型渲染成计划卡片、任务卡片、Diff 卡片、网页预览等。

这样做有两个好处。

第一，Agent 不需要知道前端 UI 怎么画，只需要按协议输出数据。  
第二，前端也不需要从自然语言里猜测哪些内容是产物，而是可以直接根据结构化数据渲染。

同时，产物和消息是绑定的。用户可以清楚看到某个 Diff、某个网页预览或某个文档，是由哪一次 Agent 回复生成的。

这个模块让 AgentHub 从“聊天回复”进一步变成了“可检查、可预览、可交付的协作工作台”。

---

## 模块五：AI 协作工程实践

第五个模块是 **AI 协作工程实践**。

我们在开发 AgentHub 的过程中，不只是让 AI 帮忙写代码，而是把团队和 AI 的协作方式沉淀到了项目里。

比如 Trellis 工作流会把需求澄清、计划、实现、验收和复盘固化下来；`.trellis/spec` 会记录后端、前端、Daemon 的编码规范；`AGENTS.md` 会记录架构事实、目录结构、命名规则和一些真实踩过的坑。

这样做的意义是：项目上下文不会只停留在一次聊天里，而是沉淀在仓库中。后续新的 Agent 或新的会话进入项目时，可以先读取这些规范，再进行开发。

这对长期项目很重要，因为 AI 协作最大的问题之一是上下文容易漂移。我们通过文档、规范、任务记录和 bugfix 记录，把协作过程变得可追溯、可复盘，也更适合持续迭代。

---

## 结尾总结

最后总结一下，AgentHub 的核心不是做一个聊天页面，而是把本地 CLI Agent 接入、IM 交互、多 Agent 调度、上下文工程和产物交付串成了一个完整闭环。

我认为最有工程含量的是这几个设计点：

第一，**Agent ID × Slot × Session**，把平台身份、本地进程和上下文隔离拆开。  
第二，**ContextChain**，把上下文工程从 Orchestrator 中抽离出来。  
第三，**Dispatcher**，统一了任务创建、WebSocket 派发、等待结果和消息落库。  
第四，**AgentQueue**，解决了同 Agent 串行、不同 Agent 并行的问题。  
第五，**产物卡片协议**，让 Agent 输出从文本变成可检查、可预览、可交付的结构化产物。

这几个点共同说明：**AgentHub 不是一个聊天壳子，而是一个面向本地 CLI Agent 的协作调度平台。**